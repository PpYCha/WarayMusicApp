import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const SongLyricsScreen = ({route}) => {
  return (
    <View>
      <Text>SongLyricsScreen</Text>
    </View>
  );
};

export default SongLyricsScreen;

const styles = StyleSheet.create({});
